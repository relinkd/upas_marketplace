type Issuer = record { verified : bool; issuer_type : text };
type Result = variant { Ok : Issuer; Err : text };
service : {
  caller : () -> (principal) query;
  getIssuer : (principal) -> (Result) query;
  isController : () -> (bool) query;
}
